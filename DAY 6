class Solution {
    public int commonFactors(int a, int b) {
        int i;
    int k=0;
    int limit=Math.max(a,b);
    for(i=1;i<=limit;i++)
    {
        if(a%i==0&&b%i==0)
        k++;
    }
    return k;
        
    }
}
