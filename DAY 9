class Solution {
    public int reverse(int x) {
       long s=0;
        int r;
       while(x!=0)
        {
            r=x%10;
            s=s*10+r;
            x/= 10;
        }
        if(s>=-2147483648&&s<=2147483647)
        return (int)s;
        else
       return 0;
        
    }
}
