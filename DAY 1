class Solution {
    public boolean isPalindrome(int x) {
        int r;int s=0;
        int n=x;
        while(x>0)
        {
        r=x%10;
        s=(s*10)+r;
        x=x/10;
        
    }
    if(n==s)
    return true;
    else
    return false;
}
}
