class Solution {
    public int addDigits(int num) {
        int r;int t=0;
        int s=0;int q;
        while(num>=10)
        {
            
            r=num%10;
            q=num/10;
            t=r+q;
            num=t;
        }
        return num;
        
    }
}
